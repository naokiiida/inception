# Debian preseed file for automated installation
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

# Network configuration
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string inception
d-i netcfg/get_domain string local

# Mirror settings
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# Account setup
d-i passwd/root-login boolean true
d-i passwd/root-password password root
d-i passwd/root-password-again password root
d-i passwd/user-fullname string Inception User
d-i passwd/username string user
d-i passwd/user-password password user
d-i passwd/user-password-again password user

# Clock and time zone setup
d-i clock-setup/utc boolean true
d-i time/zone string Japan

# Partitioning
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# Base system installation
d-i base-installer/install-recommends boolean false

# Package selection
tasksel tasksel/first multiselect standard, ssh-server
d-i pkgsel/include string openssh-server sudo curl wget git make ca-certificates gnupg lsb-release dkms linux-headers-amd64
d-i pkgsel/upgrade select safe-upgrade
d-i pkgsel/update-policy select none
popularity-contest popularity-contest/participate boolean false

# Boot loader installation
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

# Finishing up the installation
d-i finish-install/reboot_in_progress note

# Post-install commands
d-i preseed/late_command string \
    in-target usermod -aG sudo user ; \
    in-target systemctl enable ssh ; \
    if [ -b /dev/sr1 ]; then \
        mkdir -p /target/mnt/guest-additions && \
        mount /dev/sr1 /target/mnt/guest-additions && \
        in-target sh /mnt/guest-additions/VBoxLinuxAdditions.run --nox11 || true ; \
        umount /target/mnt/guest-additions || true ; \
        in-target usermod -aG vboxsf user || true ; \
    fi ; \
    in-target mkdir -p /etc/apt/keyrings ; \
    in-target curl -fsSL https://download.docker.com/linux/debian/gpg -o /etc/apt/keyrings/docker.gpg || exit 1 ; \
    in-target chmod a+r /etc/apt/keyrings/docker.gpg ; \
    in-target sh -c 'echo "deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian bookworm stable" > /etc/apt/sources.list.d/docker.list' ; \
    in-target apt-get update ; \
    in-target apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin ; \
    in-target systemctl enable docker ; \
    in-target usermod -aG docker user
